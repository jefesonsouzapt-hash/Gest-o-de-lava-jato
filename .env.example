# Copie para .env.local e preencha. .env.local é ignorado pelo git —
# nunca colocar credenciais reais no repositório.

# Conexão com o PostgreSQL (Neon, Supabase ou local).
DATABASE_URL=

# Segredo de sessão (gerar com: openssl rand -base64 32)
AUTH_SECRET=
